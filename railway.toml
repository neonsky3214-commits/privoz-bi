[build]
builder = "nixpacks"

[deploy]
startCommand = "cd backend && /app/venv/bin/python -c 'from db import init_db; init_db()' && /app/venv/bin/uvicorn main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "on_failure"
